OK, we're pursuing the new direction.
Last completed: * basic frame

Next steps:
  - create a new conversation, but trained up on softspec v3
  - have it define a data format that includes the basic elements, and also styles, but that doesn't have attachment ports, connections yet
    - also, does NOT require more than definitions
    - DO include definitions, -- I'd like it to support a library of things right now to start with
  - create some examples
  - then have the first chat render some things