QUICK NOTE (2026-08-12) on WHERE I AM RIGHT NOW:
  - When I left this off, I was verifying the line-drawing code, section by section, module by module.
  - I was on the very last part: projection.md, which we divided into projection.md, projection-tkinter.md, and projection-canvas.md.
  - And then in projection-tkinter.md, I was concerned with making sure that row selection didn't act funny because of re-activating;
   - So we were adding to interaction-runtime.md that it would RECORD which stage we were in,
   - and that when an event was received during projection, it'd just ignore it.
 - What needs to be done now:  Ensure that the change to interaction-runtime.md looks right, that interaction-runtime.md looks right generally,
   that editor-window.md looks right, and then finally that projection-tkinter.md looks right, ...
 - And then complete the overall review of projection-tkinter.md, projection-canvas.md (very important,) and I think... I think projection.md is right, too.  But check it.
  - Another concern I had was that projection receives events?  But project-canvas is reading all at once, and projection-tkinter is reading all at once?  Should it go one at a time, or does it matter?  Do they just ignore one another's events?  I don't know that I care, as long as AI sorts it out CORRECTLY, ...  But it'd be nice to know what it's doing.


2026-08-07
Mission right now: USABLE = CONNETORS + ATTACHMENT + PRESENTATION + EDITOR

Priorities now:
  1. Work out how connecting works, from an API angle.  Do you keep track of all connectors assembled so far?  Is an instance-id required?
  * THINK ABOUT: Do you want only "painting," or do you want "retained" mode.
  2. Implement attachment drawing.
  3. Organize work on isolating the core package, and then making an (also isolated) editor and demonstration runner.

percolating thought:  I need AI on demand -- both (a) small context-loadable processes doing one thing, and (b) long running agents.
  - right now, these would be involved in very specific tasks of identifying and documenting data structures